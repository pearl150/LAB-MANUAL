#include <iostream>
using namespace std;

int maxNumber(int a, int b) {
    return (a > b) ? a : b;
}

int main() {
    int a = 10, b = 20;
    cout << "Max number is: " << maxNumber(a, b) << endl;
    return 0;
}
